# For local development, leave this as is or omit the file entirely.
# For production (after deploying backend), set this in Vercel/Netlify
# environment variables to your live backend URL, e.g.:
# VITE_API_URL=https://your-backend.onrender.com
VITE_API_URL=http://localhost:5000
